Make sure QT 3.* and QWT 4.* libraries (along with their dependencies) are installed.

In ubuntu/debian releases, install as following:

o      apt-get install libqt3-mt
o      apt-get install libqwt-dev

Alternatively, download 'linux_lib-pack.tar.gz' and copy its contents to /usr/lib.

